# Optional endpoint for real waitlist collection.
# The frontend will POST JSON to this URL with:
# { email, source, submittedAt }
#
# Example:
# VITE_WAITLIST_ENDPOINT=https://example.com/api/waitlist

VITE_WAITLIST_ENDPOINT=
