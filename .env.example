# Frontend endpoint used by the waitlist form.
# On Vercel, use the local serverless function route.
VITE_WAITLIST_ENDPOINT=/api/waitlist

# Server-side only: Brevo contacts sync.
BREVO_API_KEY=
BREVO_LIST_ID=
