---
import TermLink from './TermLink.astro';

interface Props {
  slug: string;
  label?: string;
  summary?: string;
}

const { slug, label, summary } = Astro.props;
---
<TermLink slug={slug} label={label} summary={summary}>
  <slot />
</TermLink>